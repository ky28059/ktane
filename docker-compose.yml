services:
  nsjail-runner:
    build: ./nsjail
    ports:
      - 5001:5001
    privileged: true
    network_mode: "host"
    extra_hosts:
      - "host.docker.internal:host-gateway"
  
  backend:
    build: ./server
    ports:
      - 8000:8000
    network_mode: "host"
    extra_hosts:
      - "host.docker.internal:host-gateway"
  
  
  frontend:
    build: ./client
    ports:
      - 3000:3000
    network_mode: "host"
    extra_hosts:
      - "host.docker.internal:host-gateway"

  mongo:
    image: mongo:latest
    ports:
      - 27017:27017
    network_mode: "host"
    extra_hosts:
      - "host.docker.internal:host-gateway"
  
